<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Rent</title>
    <link rel="stylesheet" href="../css/stylegreen.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #0d0d0d; /* Dark background */
            color: #ffffff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            padding: 20px;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 10px rgba(0, 255, 128, 0.7);
        }

        p {
            font-size: 1.5rem;
            margin-bottom: 20px;
        }

        a {
            display: inline-block;
            background-color: #39ff14; /* Neon green button */
            color: black;
            padding: 10px 20px;
            margin: 5px;
            border-radius: 5px;
            text-decoration: none;
            transition: background-color 0.3s, transform 0.3s;
        }

        a:hover {
            background-color: #32cc11; /* Darker green on hover */
            transform: translateY(-2px); /* Slight lift on hover */
        }

        .navigation {
            margin-top: 20px;
        }
    </style>
</head>
<body>
    <h1>Room-ID: <%= rent.r_id %></h1>
    <p>Person-ID: <%= rent.p_id %></p>
    <div class="navigation">
        <a href="/update3/<%= rent.id %>">Edit</a>
        <a href="/delete3/<%= rent.id %>">Delete</a>
        <a href="/renting">Back to List</a>
    </div>
</body>
</html>
