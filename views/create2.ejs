<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Create Person</title>
    <link rel="stylesheet" href="../css/stylepurple.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #2a2a72; /* Dark purple gradient */
            background: linear-gradient(to right, #2a2a72, #009ffd);
            color: #ffffff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
        }

        h1 {
            font-size: 2.5rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 8px rgba(255, 0, 255, 0.7);
        }

        form {
            background-color: #3a3a82; /* Slightly lighter purple */
            border-radius: 10px;
            padding: 20px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
            width: 300px;
            text-align: center;
        }

        label {
            font-size: 1.2rem;
            margin-bottom: 10px;
            display: block;
        }

        input[type="text"] {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            border: 1px solid #ff5e99; /* Bright pink border */
            border-radius: 5px;
            background-color: #4a4a9a; /* Darker input background */
            color: #fff;
            transition: border-color 0.3s;
        }

        input[type="text"]:focus {
            border-color: #ff5e99;
            outline: none;
        }

        input[type="submit"] {
            background-color: #ff5e99; /* Bright pink */
            border: none;
            border-radius: 5px;
            color: white;
            padding: 10px 20px;
            font-size: 1rem;
            cursor: pointer;
            transition: background-color 0.3s;
        }

        input[type="submit"]:hover {
            background-color: #e55a8b; /* Darker pink on hover */
        }

        a {
            margin-top: 20px;
            color: #ff5e99;
            text-decoration: none;
            font-size: 1rem;
        }

        a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <h1>Create Person</h1>
    <form method="post" action="/create2">
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" placeholder="Enter your name..." required>
        <input type="submit" value="Create">
    </form>
    <a href="/people">Back to List</a>
</body>
</html>
