<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Person</title>
    <link rel="stylesheet" href="../css/stylepurple.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #3b3b6e; /* Dark purple background */
            color: #ffffff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            height: 100vh;
            margin: 0;
            padding: 20px;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 10px rgba(128, 0, 128, 0.7); /* Purple glow */
        }

        form {
            background-color: #4e4e8d; /* Form background */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(128, 0, 128, 0.5); /* Purple shadow */
            width: 300px; /* Fixed width for form */
            display: flex;
            flex-direction: column;
            gap: 15px; /* Space between inputs */
        }

        label {
            font-size: 1.2rem;
        }

        input[type="text"] {
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #6e6ea3; /* Input background */
            color: white;
            font-size: 1rem;
        }

        input[type="submit"] {
            background-color: #800080; /* Purple button */
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.3s, transform 0.3s;
        }

        input[type="submit"]:hover {
            background-color: #6a007c; /* Darker purple on hover */
            transform: translateY(-2px); /* Slight lift on hover */
        }

        a {
            color: #d5a6e0; /* Light purple link */
            text-decoration: none;
            margin-top: 20px;
            transition: color 0.3s;
        }

        a:hover {
            color: #c27ba0; /* Darker pink on hover */
        }
    </style>
</head>
<body>
    <h1>Update Person</h1>
    <form method="post" action="/update2/<%= person.id %>">
        <label for="name">Name:</label>
        <input type="text" id="name" name="name" value="<%= person.name %>" required>
        <input type="submit" value="Update">
    </form>
    <a href="/people">Back to List</a>
</body>
</html>
