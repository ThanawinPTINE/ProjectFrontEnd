<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Rent</title>
    <link rel="stylesheet" href="../css/stylegreen.css">
    <style>
        body {
            font-family: 'Arial', sans-serif;
            background-color: #1b3a6d; /* Dark blue background */
            color: #ffffff;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: flex-start;
            height: 100vh;
            margin: 0;
            padding: 20px;
        }

        h1 {
            font-size: 3rem;
            margin-bottom: 20px;
            text-shadow: 2px 2px 10px rgba(0, 255, 0, 0.7); /* Green glow */
        }

        form {
            background-color: #2c4d82; /* Form background */
            padding: 20px;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 255, 0, 0.5); /* Green shadow */
            width: 300px; /* Fixed width for form */
            display: flex;
            flex-direction: column;
            gap: 15px; /* Space between inputs */
        }

        label {
            font-size: 1.2rem;
        }

        input[type="number"] {
            padding: 10px;
            border: none;
            border-radius: 5px;
            background-color: #4a6a95; /* Input background */
            color: white;
            font-size: 1rem;
        }

        input[type="submit"] {
            background-color: #28a745; /* Green button */
            color: white;
            padding: 10px;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            font-size: 1rem;
            transition: background-color 0.3s, transform 0.3s;
        }

        input[type="submit"]:hover {
            background-color: #218838; /* Darker green on hover */
            transform: translateY(-2px); /* Slight lift on hover */
        }

        a {
            color: #77dd77; /* Light green link */
            text-decoration: none;
            margin-top: 20px;
            transition: color 0.3s;
        }

        a:hover {
            color: #5e9e5e; /* Darker green on hover */
        }
    </style>
</head>
<body>
    <h1>Update Rent</h1>
    <form method="post" action="/update3/<%= rent.id %>">
        <label for="r_id">Room-ID:</label>
        <input type="number" id="r_id" name="r_id" value="<%= rent.r_id %>" required>
        
        <label for="p_id">Person-ID:</label>
        <input type="number" id="p_id" name="p_id" value="<%= rent.p_id %>" required>
        
        <input type="submit" value="Update">
    </form>
    <a href="/renting">Back to List</a>
</body>
</html>
